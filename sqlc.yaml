version: 2
sql:
  - engine: postgresql
    schema: "db/migrations/"
    queries: "db/queries/"
    gen:
      go:
        out: "db/generated"
        package: "db"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_empty_slices: true
        emit_json_tags: true